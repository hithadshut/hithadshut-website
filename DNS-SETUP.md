# הגדרת DNS לדומיין hithadshut.co.il

מדריך זה מסביר איך לחבר את הדומיין `hithadshut.co.il` שנרכש ב-Internic לאתר המתארח ב-Vercel.

---

## שלב 1 — כניסה לפאנל ניהול הדומיין ב-Internic

1. היכנסו לאתר [https://www.internic.co.il](https://www.internic.co.il)
2. לחצו על **"התחברות"** (מימין למעלה)
3. הזינו את שם המשתמש והסיסמה שקיבלתם בעת רכישת הדומיין
4. לאחר ההתחברות — לחצו על **"הדומיינים שלי"** / **"ניהול דומיינים"**
5. בחרו את הדומיין `hithadshut.co.il`
6. לחצו על **"ניהול DNS"** או **"עריכת רשומות DNS"**

---

## שלב 2 — הוספת רשומות DNS

יש להוסיף **שתי רשומות** בפאנל הניהול:

### רשומה ראשונה — A record (לדומיין הראשי)

| שדה | ערך |
|-----|-----|
| **Type / סוג** | `A` |
| **Name / Host / שם** | `@` (או להשאיר ריק — מייצג את הדומיין הראשי) |
| **Value / כתובת יעד** | `76.76.21.21` |
| **TTL** | `3600` |

### רשומה שנייה — CNAME record (לתת-דומיין www)

| שדה | ערך |
|-----|-----|
| **Type / סוג** | `CNAME` |
| **Name / Host / שם** | `www` |
| **Value / כתובת יעד** | `cname.vercel-dns.com` |
| **TTL** | `3600` |

**חשוב:** אם יש רשומות A או CNAME קיימות על `@` או `www` — יש למחוק אותן לפני הוספת החדשות.

לאחר ההזנה — לחצו על **"שמור"** / **"Save"**.

---

## שלב 3 — אימות ב-Vercel

1. היכנסו ל-[Vercel Dashboard](https://vercel.com/dashboard)
2. בחרו את הפרויקט
3. גשו ל-**Settings → Domains**
4. בדקו שהדומיינים `hithadshut.co.il` ו-`www.hithadshut.co.il` מופיעים ברשימה
5. Vercel יציג **"Valid Configuration"** ליד כל אחד לאחר שה-DNS התעדכן

---

## שלב 4 — זמני המתנה

- עדכון DNS יכול לקחת **בין כמה דקות ל-24 שעות** (בדרך כלל עד שעה).
- אפשר לבדוק את הסטטוס באתר [https://dnschecker.org](https://dnschecker.org) — הכניסו `hithadshut.co.il` ובחרו `A` או `CNAME`.
- כאשר תראו את ה-IP `76.76.21.21` מופיע ברוב השרתים — החיבור עבד.

---

## שלב 5 — בדיקה סופית

לאחר שה-DNS התעדכן:

1. פתחו דפדפן והיכנסו ל-`https://hithadshut.co.il`
2. בדקו שהאתר נטען כצפוי
3. בדקו גם `https://www.hithadshut.co.il` — צריך להפנות אוטומטית ל-`hithadshut.co.il`
4. ודאו שה-SSL (מנעול ירוק) תקין — Vercel מנפיק תעודת SSL אוטומטית

---

## פתרון בעיות (Troubleshooting)

### האתר לא נטען אחרי 24 שעות

- בדקו ב-[dnschecker.org](https://dnschecker.org) שהרשומות אכן מופצות.
- ודאו ש-Vercel מציג **"Valid Configuration"** בדף Domains.
- ודאו שלא נשארו רשומות ישנות סותרות בפאנל Internic.

### שגיאת SSL

- Vercel מנפיק תעודת SSL אוטומטית לאחר שה-DNS תקין. יש להמתין עד 30 דקות אחרי האימות.
- אם השגיאה נמשכת — לחצו ב-Vercel על **"Refresh"** ליד שם הדומיין.

### www לא מפנה לדומיין הראשי

- ודאו שרשומת ה-CNAME ל-`www` מצביעה במדויק על `cname.vercel-dns.com` (כולל הנקודה בסוף אם הפאנל דורש זאת).
- ב-Vercel — בדקו שהוגדר Redirect אוטומטי מ-`www` לדומיין הראשי.

### לא בטוחים מה לעשות?

ניתן לפנות לתמיכה של Internic בטלפון או בדוא״ל המופיעים באתר, ולבקש מהם להגדיר את שתי הרשומות לפי הפרטים בטבלאות למעלה.

---

**עודכן לאחרונה:** אפריל 2026
